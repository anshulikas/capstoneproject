# capstoneproject


## Install
* Run following command inside project root directory.
```
npm install
```

## Start
* Run following command to start sever
```
npm start
```
* By default server takes port 5000, go to [http://localhost:5000](http://localhost:5000)